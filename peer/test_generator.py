# wallet1 = Wallet()._generate_signing_key()
# wallet2 = Wallet()._generate_signing_key()

# wallet3 = Wallet()._generate_signing_key()
# wallet4 = Wallet()._generate_signing_key()

# wallet5 = Wallet()._generate_signing_key()
# wallet6 = Wallet()._generate_signing_key()

# wallet7 = Wallet()._generate_signing_key()
# wallet8 = Wallet()._generate_signing_key()

# wallet9 = Wallet()._generate_signing_key()
# wallet10 = Wallet()._generate_signing_key()

# blockchain.add_transaction(wallet1.signingKey, wallet2.public_key, 50)

# blockchain.add_transaction(wallet3.signingKey, wallet4.public_key, 50)

# blockchain.add_transaction(wallet6.signingKey, wallet5.public_key, 50)

# blockchain.mine_block(wallet6.signingKey)

# blockchain.add_transaction(wallet8.signingKey, wallet7.public_key, 50)

# blockchain.add_transaction(wallet8.signingKey, wallet6.public_key, 50)

# blockchain.add_transaction(wallet4.signingKey, wallet6.public_key, 50)

# blockchain.add_transaction(wallet8.signingKey, wallet6.public_key, 50)

# blockchain.mine_block(wallet8.signingKey)

# blockchain.add_transaction(wallet10.signingKey, wallet6.public_key, 50)

# blockchain.add_transaction(wallet9.signingKey, wallet10.public_key, 50)

# blockchain.mine_block(wallet8.signingKey)